# =============================================================================
# Imperishable Gate Server Environment Configuration File
# Imperishable Gate 服务端环境变量配置文件
# =============================================================================
# Copy this file to .env and modify with actual configuration values
# Place this file in the cmd/gate-server/ directory
#
# 复制此文件为 .env 并修改为实际配置值
# 将此文件放在 cmd/gate-server/ 目录下
#
# For client configuration, see: cmd/gate/.env.example
# 客户端配置请参考：cmd/gate/.env.example

# =============================================================================
# Database Configuration
# 数据库配置
# =============================================================================
# Database type: sqlite (default), mysql, postgres
# 数据库类型：sqlite（默认）、mysql、postgres
DB_TYPE=sqlite

# Database connection string (DSN)
# SQLite (default): gate.db or /path/to/gate.db
# 数据库连接字符串（DSN）
# SQLite（默认）: gate.db 或 /path/to/gate.db
DSN=gate.db

# MySQL example:
# MySQL 示例:
# DB_TYPE=mysql
# DSN=user:password@tcp(127.0.0.1:3306)/gate_db?charset=utf8mb4&parseTime=True&loc=Local

# PostgreSQL example:
# PostgreSQL 示例:
# DB_TYPE=postgres
# DSN=host=localhost user=postgres password=postgres dbname=gate_db port=5432 sslmode=disable TimeZone=Asia/Shanghai

# =============================================================================
# Server Configuration
# 服务器配置
# =============================================================================
# Server listening address and port
# Format: <host>:<port>
# 服务器监听地址和端口
# 格式: <主机>:<端口>
SERVER_ADDR=localhost:4514

# =============================================================================
# JWT Security Configuration
# JWT 安全配置
# =============================================================================
# JWT signing secret key (strongly recommend using a complex random string in production)
# You can generate a random key using: openssl rand -base64 64
# JWT 签名密钥（强烈建议在生产环境中使用复杂的随机字符串）
# 可以使用 openssl rand -base64 64 生成随机密钥
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# =============================================================================
# Logging Configuration
# 日志配置
# =============================================================================
# Log level: debug, info, warn, error, silent
# - debug: Log all SQL queries and debug information (for development debugging only)
# - info: Log general information and slow queries (development environment)
# - warn: Log only warnings, errors, and slow queries (default, recommended for production)
# - error: Log only errors
# - silent: No database logs output
# Default: warn
# 日志级别：debug, info, warn, error, silent
# - debug: 记录所有 SQL 查询和调试信息（仅用于开发调试）
# - info: 记录一般信息和慢查询（开发环境）
# - warn: 仅记录警告、错误和慢查询（默认，推荐生产环境）
# - error: 仅记录错误信息
# - silent: 不输出任何数据库日志
# 默认值：warn
LOG_LEVEL=warn

# =============================================================================
# Email Service Configuration (for link monitoring change notifications)
# 邮件服务配置（用于链接监控变化通知）
# =============================================================================
# SMTP server address
# SMTP 服务器地址
EMAIL_HOST=smtp.example.com

# SMTP server port
# - Port 465: SSL/TLS encryption (recommended, works with most cloud servers)
# - Port 587: STARTTLS encryption (recommended)
# - Port 25: Plain text transmission (not recommended, often blocked by cloud servers)
# SMTP 服务器端口
# - 端口 465: SSL/TLS 加密（推荐，适用于大多数云服务器）
# - 端口 587: STARTTLS 加密（推荐）
# - 端口 25: 明文传输（不推荐，且多数云服务器会封禁此端口）
EMAIL_PORT=465

# Sender email address
# 发件人邮箱地址
EMAIL_FROM=noreply@example.com

# Email password or authorization code (some email services require authorization codes instead of passwords)
# NetEase 163/126: Enable SMTP service in email settings and generate an authorization code
# QQ Mail: Enable SMTP service in email settings and generate an authorization code
# Gmail: Use an app-specific password
# 邮箱密码或授权码（部分邮箱服务需使用授权码而非密码）
# 网易163/126邮箱: 需要在邮箱设置中开启SMTP服务并生成授权码
# QQ邮箱: 需要在邮箱设置中开启SMTP服务并生成授权码
# Gmail: 需要使用应用专用密码
EMAIL_PASSWORD=your-email-password-or-app-password
